export default class Camera {
    x: number;
    y: number;

    constructor(x: number, y: number){
        this.x      = x; // Variables de desplazamiento de camara en X
        this.y      = y; // Variables de desplazamiento en Y
    }
}
